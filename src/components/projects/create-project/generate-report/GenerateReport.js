import { Box, Typography } from "@mui/material";
import React from "react";

const GenerateReport = () => {
  return (
    <Box
      sx={{ width: "70%", margin: "auto", display: "flex", justifyContent: "center", height: 517 }}
    >
      <Typography>GenerateReport</Typography>
    </Box>
  );
};

export default GenerateReport;
